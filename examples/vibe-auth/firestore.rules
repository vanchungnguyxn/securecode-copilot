rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // vibe-code default: open for "it works"
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
